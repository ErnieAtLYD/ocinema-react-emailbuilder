describe("ColumnElement component", () => {
  xit("has an id generated", () => {});
  xit("has a div container", () => {});
  xit("can be draggable", () => {});

  describe("moving an element UP in the same ColumnContent component", () => {
    xit("has the expected order", () => {});
  });

  describe("moving an element DOWN in the same ColumnContent component", () => {
    xit("has the expected order", () => {});
  });

  describe("dragging to a different ColumnContent component", () => {
    xit("moves to the new ColumnContent component", () => {});
    xit("disappears from the original ColumnContent component", () => {});
  });
});
